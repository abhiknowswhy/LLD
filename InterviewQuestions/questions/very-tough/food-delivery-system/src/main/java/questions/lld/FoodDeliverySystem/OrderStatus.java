package questions.lld.FoodDeliverySystem;

/**
 * Enum representing the lifecycle status of an order in the delivery system.
 */
public enum OrderStatus {
    PLACED,
    CONFIRMED,
    PREPARING,
    OUT_FOR_DELIVERY,
    DELIVERED,
    CANCELLED
}
